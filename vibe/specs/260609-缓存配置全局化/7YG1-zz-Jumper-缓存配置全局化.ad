= 缓存配置全局化 [[缓存配置全局化-7YG1-Jumper]]

* [ ] 全局缓存
** EzEditorJumper 项目级配置: work-space/project的 file-path 进行唯一code生成, 作为配置主键
*** 使用全局配置保存, 以配置主键作为缓存key
*** 全局配置不保存在工作区配置中, 而保存到mac/win对应合适的缓存目录
*** 缓存文件名以配置主键作为前缀, 然后加上workspace名/项目文件夹名, 再加上插件名, 以避免冲突
*** vs项目路径可选文件夹, 也可指定文件(如vs的workspace文件)
** 启动软件级配置, 也使用全局缓存, 和EzEditorJumperV共用一套配置
*** 所有配置的app变更, 如路径指定后扫描验证, 所有项目的本插件共用一套启动软件配置
*** 新增的app类型也统一保存

## 运行时修复：Settings 打开 StackOverflowError（v2.0.2）

**现象**：打开 IDE Settings / macOS 系统偏好时，`EditorJumperProjectSettings.loadState` ↔ `ProjectConfigStore.readProject` 循环调用 `getInstance`，导致 `StackOverflowError`。日志中 `Plugin to blame: EzEditorJumper version: 2.0.2`。

**根因**：`loadState` 在无 legacy XML 时调用 `store.readProject()`；`readProject` 经 `resolveAnchorPath` 再次 `getInstance(EditorJumperProjectSettings)`，在 PSC 初始化期间重入 `loadState`。

**修复**（相对缓存全局化 baseline）：
- `ProjectConfigStore.resolveAnchorPath`：仅用 `legacyWorkspacePath` 或 `project.basePath`，不再调用 `EditorJumperProjectSettings.getInstance`。
- `EditorJumperProjectSettings.loadState`：仅在有 legacy XML 数据时迁移写入 cache；**禁止**在 `loadState` 中调用 `readProject()`。
- `EditorJumperStartupActivity`：启动时从 cache 读取后，将空白字段回填到 `EditorJumperProjectSettings` 内存态。

**无关日志**：`PluginLogo - Cannot load plugin icon ... Not a directory` 来自 Gradle 缓存中 Android 插件 JAR，与 EzEditorJumper 无关。

### 测试清单（StackOverflow 修复）

| 场景 | 状态 |
|------|------|
| `./gradlew test --tests PathKeyUtilTest` + `compileKotlin` | 已通过 |
| IDE 启动后打开 Settings / macOS 偏好（无 StackOverflow） | 待本机重载插件验证 |
| 项目配置读写、状态栏、外部编辑器跳转 | 待本机重载插件验证 |
